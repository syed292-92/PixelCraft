// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
import java.awt.image.BufferedImage;
import java.io.IOException;

public class Invert extends Converter {
   public Invert() {
   }

   public void convert(String var1, String var2) throws IOException {
      BufferedImage var3 = this.loadImage(var1);

      for(int var4 = 0; var4 < var3.getHeight(); ++var4) {
         for(int var5 = 0; var5 < var3.getWidth(); ++var5) {
            ARGB var6 = new ARGB(var3.getRGB(var5, var4));
            var3.setRGB(var5, var4, (new ARGB(var6.alpha, 255 - var6.red, 255 - var6.green, 255 - var6.blue)).toInt());
         }
      }

      this.saveImage(var3, var2);
   }
}
